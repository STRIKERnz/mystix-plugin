import_roadmaps:
  name: Import roadmaps
  description: Replace a player's roadmaps with a JSON list.
  fields:
    player:
      name: Player
      required: true
      selector:
        text:
    roadmaps_json:
      name: Roadmaps JSON
      required: true
      selector:
        text:
          multiline: true

clear_roadmaps:
  name: Clear roadmaps
  description: Remove all locally stored roadmaps for a player.
  fields:
    player:
      name: Player
      required: true
      selector:
        text:
